import React from 'react';
import { Typography } from '@mui/material';
export default function Group() {
  return <Typography>Quản lý nhóm đồng sở hữu (thành viên, quyền, bỏ phiếu, quỹ chung).</Typography>;
}